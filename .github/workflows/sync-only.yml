name: Sync upstream (master) to fork

on:
  schedule:
    - cron: '*/15 * * * *' # запуск каждые 15 минут
  workflow_dispatch:
    inputs:
      mode:
        description: 'Режим работы (auto/test/resend)'
        required: false
        default: 'auto'

env:
  UPSTREAM_OWNER: muk-as
  UPSTREAM_REPO: DOTA2_WEB
  UPSTREAM_BRANCH: master
  FORK_BRANCH: master

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 🧩 Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 💾 Save current fork SHA
        id: before
        run: echo "before_sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: 🔗 Add upstream and fetch
        run: |
          git remote add upstream https://github.com/${{ env.UPSTREAM_OWNER }}/${{ env.UPSTREAM_REPO }}.git || true
          git fetch --no-tags upstream

      - name: ⚙️ Configure git identity
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: 🔄 Merge upstream into fork branch
        id: merge
        run: |
          git checkout ${{ env.FORK_BRANCH }} || git switch ${{ env.FORK_BRANCH }}
          if git merge --ff-only upstream/${{ env.UPSTREAM_BRANCH }} -m "Fast-forward merge upstream/${{ env.UPSTREAM_BRANCH }}" 2>/dev/null; then
            echo "merged=ff" >> $GITHUB_OUTPUT
            echo "✅ Fast-forward merge выполнен."
          else
            echo "ℹ️ Fast-forward невозможен, выполняется обычный merge..."
            git merge --no-edit upstream/${{ env.UPSTREAM_BRANCH }} || true
            echo "merged=merge" >> $GITHUB_OUTPUT
          fi

      - name: 💾 Save new SHA
        id: after
        run: echo "after_sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: 🚀 Compare SHAs and push if changed
        id: push
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ steps.before.outputs.before_sha }}" != "${{ steps.after.outputs.after_sha }}" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            git push https://x-access-token:${TOKEN}@github.com/${{ github.repository }} HEAD:${{ env.FORK_BRANCH }}
            echo "✅ Форк успешно синхронизирован с upstream."
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Нет изменений — всё уже актуально."
          fi

      # -----------------------------------------------
      # 📢 Отправка Discord уведомлений (полный, безопасный блок)
      # -----------------------------------------------
      - name: 📢 Send Dota updates to Discord
        if: steps.push.outputs.changed == 'true' || github.event.inputs.mode != ''
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          set -euo pipefail
          # set -x  # Включи временно для debug

          MODE="${{ github.event.inputs.mode || 'auto' }}"
          echo "📢 Mode: $MODE"

          # 🧪 Тестовый режим
          if [[ "$MODE" == "test" ]]; then
            echo "🧪 Sending test message..."
            embed_title="🧪 Dota 2 Test Notification"
            embed_desc=$'**Тип обновления:** Dota 2 Test Mode\n\n**Что нового:**\n[v] test 12345 => 12346\n[h] testhash123 => testhash456\n[s] test CN hero => hero2\n\n🔗 Посмотреть коммит'
            jq -n \
              --arg title "$embed_title" \
              --arg desc "$embed_desc" \
              '{embeds: [{title: $title, description: $desc, color: 5814783}]}' \
            | curl -s -H "Content-Type: application/json" -X POST -d @- "$DISCORD_WEBHOOK"
            exit 0
          fi

          # -----------------------------
          # Санайтизация SHA (берём значения из шагов)
          # -----------------------------
          before_raw="${{ steps.before.outputs.before_sha }}"
          after_raw="${{ steps.after.outputs.after_sha }}"
          before=$(printf '%s' "$before_raw" | tr -d '\r\n' | awk '{$1=$1;print}')
          after=$(printf '%s' "$after_raw" | tr -d '\r\n' | awk '{$1=$1;print}')
          echo "DEBUG: before='$before' after='$after'"

          # -----------------------------
          # Получаем upstream (на всякий случай делаем глубокий fetch)
          # -----------------------------
          git remote set-url upstream "https://github.com/${{ env.UPSTREAM_OWNER }}/${{ env.UPSTREAM_REPO }}.git" || true
          git fetch --no-tags upstream || true
          if ! git rev-parse "$before" >/dev/null 2>&1 || ! git rev-parse "$after" >/dev/null 2>&1; then
            echo "⚠️ SHA недоступны — выполняется fetch --unshallow..."
            git fetch --no-tags --unshallow upstream || true
            git fetch --no-tags upstream --depth=1000 || true
          fi

          # -----------------------------
          # Формирование диапазона коммитов
          # -----------------------------
          if git rev-parse "$before" >/dev/null 2>&1 && git rev-parse "$after" >/dev/null 2>&1; then
            range="$before..$after"
            echo "✅ Используем диапазон $range"
            echo "DEBUG: range for git log is $range"
            log_range=$(git log --pretty=format:"%H" "$range" --reverse || true)
          else
            echo "⚠️ Используем последние 5 коммитов вместо диапазона."
            log_range=$(git log -n 5 --pretty=format:"%H" --reverse || true)
          fi

          # -----------------------------
          # Режим resend: последняя правка _HISTORY_by_date.txt
          # -----------------------------
          if [[ "$MODE" == "resend" ]]; then
            echo "🔁 Режим пересылки последнего коммита с изменениями в _HISTORY_by_date.txt..."
            log_range=$(git log -n 10 --pretty=format:"%H" -- _HISTORY_by_date.txt || true)
            if [ -z "$log_range" ]; then
              echo "⚠️ Не найдено коммитов, изменяющих _HISTORY_by_date.txt — выходим."
              exit 0
            fi
          fi

          # Если диапазон пустой, берём последние 5 как запасной вариант
          if [ -z "${log_range:-}" ]; then
            echo "⚠️ log_range пустой — используем последние 5 коммитов."
            log_range=$(git log -n 5 --pretty=format:"%H" --reverse || true)
          fi

          echo "🔍 Обрабатываем коммиты..."
          while read -r hash; do
            [ -z "$hash" ] && continue

            title=$(git show -s --format=%s "$hash" || true)
            body=$(git show -s --format=%b "$hash" || true)
            commit_url="https://github.com/${{ github.repository }}/commit/$hash"

            # Определяем тип обновления
            if echo "$title" | grep -iq "staging"; then
              type_update="Dota 2 Staging"
            elif echo "$title" | grep -iq "experimental"; then
              type_update="Dota 2 Experimental"
            elif echo "$title" | grep -iq "heroes"; then
              type_update="Dota 2 Heroes"
            else
              type_update="Dota 2 Update"
            fi

            # -----------------------------
            # Извлечение изменений конкретно из _HISTORY_by_date.txt
            # Защищаемся от merge-коммитов и broken pipe
            # -----------------------------
            if git show -s --format=%P "$hash" | grep -q ' '; then
              # У коммита несколько родителей — скорее всего merge, diff в таком коммите не подходит
              echo "ℹ️ Пропускаем merge-коммит (нет diff): $hash"
              changes="(merge-коммит — без diff)"
            else
              # Ограничиваем вывод только изменением в файле _HISTORY_by_date.txt
              # Используем || true и перенаправляем stderr, чтобы не падать на broken pipe
              changes=$(git show "$hash" -- _HISTORY_by_date.txt 2>/dev/null \
                | grep -E "^[+-]" \
                | grep -vE "^\+\+\+|^---" \
                | head -n 10 2>/dev/null || true)
              [ -z "$changes" ] && changes="(мелкие изменения)"
            fi

            # Формируем embed
            embed_title="🧩 ${type_update}"
            embed_desc=$(printf '**Тип обновления:** %s\n\n**Что нового:**\n%s\n\n🔗 [Посмотреть коммит](%s)' "$type_update" "$changes" "$commit_url")

            # Отправляем в Discord
            jq -n \
              --arg title "$embed_title" \
              --arg desc "$embed_desc" \
              '{embeds: [{title: $title, description: $desc, color: 3066993}]}' \
            | curl -s -H "Content-Type: application/json" -X POST -d @- "$DISCORD_WEBHOOK"

            echo "✅ Sent update for $hash"
          done <<< "$log_range"