name: Sync upstream (master) to fork
on:
  schedule:
    - cron: '*/15 * * * *' # запуск каждые 15 минут
  workflow_dispatch:
    inputs:
      mode:
        description: 'Режим работы (auto/test/resend или resend -n X, где X - количество коммитов)'
        required: false
        default: 'auto'
env:
  UPSTREAM_OWNER: muk-as
  UPSTREAM_REPO: DOTA2_WEB
  UPSTREAM_BRANCH: master
  FORK_BRANCH: master
jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 🧩 Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 💾 Save current fork SHA
        id: before
        run: echo "before_sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
      - name: 🔗 Add upstream and fetch
        run: |
          git remote add upstream https://github.com/${{ env.UPSTREAM_OWNER }}/${{ env.UPSTREAM_REPO }}.git || true
          git fetch --no-tags upstream
      - name: ⚙️ Configure git identity
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: 🔄 Merge upstream into fork branch
        id: merge
        run: |
          git checkout ${{ env.FORK_BRANCH }} || git switch ${{ env.FORK_BRANCH }}
          if git merge --ff-only upstream/${{ env.UPSTREAM_BRANCH }} -m "Fast-forward merge upstream/${{ env.UPSTREAM_BRANCH }}" 2>/dev/null; then
            echo "merged=ff" >> $GITHUB_OUTPUT
            echo "✅ Fast-forward merge выполнен."
          else
            echo "ℹ️ Fast-forward невозможен, выполняется обычный merge..."
            git merge --no-edit upstream/${{ env.UPSTREAM_BRANCH }} || true
            echo "merged=merge" >> $GITHUB_OUTPUT
          fi
      - name: 💾 Save new SHA
        id: after
        run: echo "after_sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
      - name: 🚀 Compare SHAs and push if changed
        id: push
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ steps.before.outputs.before_sha }}" != "${{ steps.after.outputs.after_sha }}" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            git push https://x-access-token:${TOKEN}@github.com/${{ github.repository }} HEAD:${{ env.FORK_BRANCH }}
            echo "✅ Форк успешно синхронизирован с upstream."
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Нет изменений — всё уже актуально."
          fi

      # -----------------------------------------------
      # 📢 Отправка Discord уведомлений (исправлено форматирование)
      # -----------------------------------------------
      - name: 📢 Send Dota updates to Discord
        if: steps.push.outputs.changed == 'true' || github.event.inputs.mode != ''
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          set -euo pipefail
          MODE="${{ github.event.inputs.mode || 'auto' }}"
          echo "📢 Mode: $MODE"

          if [[ "$MODE" == "test" ]]; then
            echo "🧪 Sending test message..."
            embed_title="🧪 Dota 2 Test Notification"
            embed_desc=$'**Что нового:**\nDota 2 Staging Server | [v] 3135 => 3136\nDota 2 Staging | [v] 3135 => 3136\n\n**Дата обновления:** 2025-10-23 23:10:08\n**Источник:** [Посмотреть](https://example.com/commit)'
            color=5814783
            jq -n \
              --arg title "$embed_title" \
              --arg desc "$embed_desc" \
              --argjson color "$color" \
              '{embeds: [{title: $title, description: $desc, color: $color}]}' \
            | curl -s -H "Content-Type: application/json" -X POST -d @- "$DISCORD_WEBHOOK"
            exit 0
          fi

          # -----------------------------
          # Санайтизация SHA
          # -----------------------------
          before_raw="${{ steps.before.outputs.before_sha }}"
          after_raw="${{ steps.after.outputs.after_sha }}"
          before=$(printf '%s' "$before_raw" | tr -d '\r\n' | awk '{$1=$1;print}')
          after=$(printf '%s' "$after_raw" | tr -d '\r\n' | awk '{$1=$1;print}')
          echo "DEBUG: before='$before' after='$after'"

          # -----------------------------
          # Получаем upstream
          # -----------------------------
          git remote set-url upstream "https://github.com/${{ env.UPSTREAM_OWNER }}/${{ env.UPSTREAM_REPO }}.git" || true
          git fetch --no-tags upstream || true
          if ! git rev-parse "$before" >/dev/null 2>&1; then
            echo "⚠️ SHA недоступны — выполняется fetch --unshallow..."
            git fetch --no-tags --unshallow upstream || true
            git fetch --no-tags upstream --depth=1000 || true
          fi

          # -----------------------------
          # Формирование диапазона коммитов (только upstream)
          # -----------------------------
          range="$before..upstream/${{ env.UPSTREAM_BRANCH }}"
          echo "✅ Используем диапазон $range (только upstream изменения)"
          log_range=$(git log --pretty=format:"%H" "$range" --reverse || true)
          if [ -z "${log_range:-}" ]; then
            echo "⚠️ log_range пустой — используем последние 5 коммитов из upstream."
            log_range=$(git log -n 5 --pretty=format:"%H" "upstream/${{ env.UPSTREAM_BRANCH }}" --reverse || true)
          fi

          # -----------------------------
          # Режим resend
          # -----------------------------
          if [[ "$MODE" == resend* ]]; then
            echo "🔁 Режим пересылки последних коммитов с изменениями в _HISTORY_by_date.txt..."
            num_commits=10
            if [[ "$MODE" =~ -n\ ([0-9]+) ]]; then
              num_commits="${BASH_REMATCH[1]}"
            fi
            echo "Используем -n $num_commits коммитов."
            log_range=$(git log -n "$num_commits" --pretty=format:"%H" "upstream/${{ env.UPSTREAM_BRANCH }}" -- steam_api/_HISTORY_by_date.txt || true)
            if [ -z "$log_range" ]; then
              echo "⚠️ Не найдено коммитов, изменяющих _HISTORY_by_date.txt — выходим."
              exit 0
            fi
          fi

          echo "🔍 Обрабатываем коммиты..."
          for hash in $log_range; do
            [ -z "$hash" ] && continue
            # Пропускаем merge-коммиты
            if git show -s --format=%P "$hash" | grep -q ' '; then
              echo "ℹ️ Пропускаем merge-коммит: $hash"
              continue
            fi
            title=$(git show -s --format=%s "$hash" || true)
            commit_url="https://github.com/${{ env.UPSTREAM_OWNER }}/${{ env.UPSTREAM_REPO }}/commit/$hash"

            # -----------------------------
            # Извлечение ТОЛЬКО новых строк (+) из _HISTORY_by_date.txt, фильтр по "Dota"
            # -----------------------------
            new_lines=$(git show "$hash" -- steam_api/_HISTORY_by_date.txt 2>/dev/null \
              | grep "^\+" \
              | grep -vE "^\+\+\+" \
              | sed 's/^\+//' \
              | grep -i "dota" || true)
            if [ -z "$new_lines" ]; then
              echo "ℹ️ Нет новых строк с 'Dota' в _HISTORY_by_date.txt для $hash — пропускаем."
              continue
            fi

            # -----------------------------
            # Определяем тип обновления
            # -----------------------------
            type_update="Dota 2 Update"
            emoji="🔄"
            color=3066993
            if echo "$new_lines" | grep -iq "experimental"; then
              type_update="Dota 2 Experimental"
              emoji="🧪"
              color=16711680
            elif echo "$new_lines" | grep -iq "staging"; then
              type_update="Dota 2 Staging"
              emoji="🛠️"
              color=3447003
            elif echo "$title" | grep -iq "heroes"; then
              type_update="Dota 2 Heroes"
              emoji="🦸"
              color=15844367
            fi

            # -----------------------------
            # Парсим строки для форматирования "Что нового" и извлечения даты
            # -----------------------------
            changes=""
            date_update=""
            while IFS= read -r line; do
              # Парсим: app_id - app_name | [v] from => to | date (UTC+3)
              app_name=$(echo "$line" | awk -F' - ' '{print $2}' | awk -F' \\| ' '{print $1}')
              version_change=$(echo "$line" | awk -F' \\| ' '{print $2}' | awk -F' \\| ' '{print $1}')
              date_raw=$(echo "$line" | awk -F' \\| ' '{print $3}' | sed 's/ (UTC+3)//')
              # === КЛЮЧЕВОЕ ИЗМЕНЕНИЕ: добавляем реальный перевод строки через $'\n' ===
              changes+="${app_name} | ${version_change}"$'\n'
              if [ -z "$date_update" ]; then
                date_update="$date_raw"
              fi
            done <<< "$new_lines"

            # Формируем desc (printf сохранит реальные переводы строк)
            embed_desc=$(printf '**Что нового:**\n%s\n**Дата обновления:** %s\n**Источник:** [Посмотреть](%s)' "$changes" "$date_update" "$commit_url")

            embed_title="$emoji $type_update"
            jq -n \
              --arg title "$embed_title" \
              --arg desc "$embed_desc" \
              --argjson color "$color" \
              '{embeds: [{title: $title, description: $desc, color: $color}]}' \
            | curl -s -H "Content-Type: application/json" -X POST -d @- "$DISCORD_WEBHOOK"
            echo "✅ Sent update for $hash"
          done
